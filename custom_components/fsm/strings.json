{
  "title": "Finite State Machine",
  "config": {
    "step": {
      "user": {
        "title": "FSM is YAML-configured",
        "description": "This integration is configured via YAML only. Please add FSMs to your `configuration.yaml`."
      }
    },
    "abort": {
      "yaml_only": "This integration is configured via YAML only. Please add FSMs to your `configuration.yaml`.",
      "already_configured": "This FSM is already configured."
    }
  }
}